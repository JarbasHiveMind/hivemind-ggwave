name: Build Tests

on:
  push:
    branches: [dev, master]
  pull_request:
    branches: [dev, master]

jobs:
  build-tests:
    uses: OpenVoiceOS/gh-automations/.github/workflows/build-tests.yml@dev
